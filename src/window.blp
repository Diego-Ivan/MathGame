using Gtk 4.0;
using Adw 1;

template $MathGameWindow : Adw.ApplicationWindow {
  default-width: 600;
  default-height: 300;
  content: Adw.NavigationView {
    $MathGameStartView {
      tag: "start-view";
    }
  };
}
